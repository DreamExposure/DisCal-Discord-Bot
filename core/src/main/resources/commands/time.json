{
  "name": "time",
  "description": "Displays the current time as seen by the calendar's timezone",
  "options": [
    {
      "name": "calendar",
      "type": 4,
      "description": "The calendar to pull the time from. Defaults to 1",
      "required": false
    }
  ],
  "default_permissions": true
}
